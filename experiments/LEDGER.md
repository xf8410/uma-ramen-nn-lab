- [EXP-001a] 2026-09-01 | 口径 | 手写基线在 pin SHA 43f532c 可测得稳定数字，作为本仓锚点 | CI experiment job：ramen_space_bench 手写策略 300 局（采样空间=教师采集同构），输出均分/标准误/达标率；与旧锚点 65445 对比只作参考不裁决（跨 commit 不可比，§0.2） | 待跑 | 产出 = 本仓正式锚点，写回 LEDGER 新条目；此后一切实验以它为对照 | **修订 EXP-001**：旧管线 checkpoint 不可得（用户 2026-09-01 确认），集成复现取消；上游引用改为 SHA pin 43f532c（UPSTREAM.md），CI 由 floating ref 改 pin
- [EXP-001 修订] 2026-09-01 | 口径 | （见 EXP-001a） | 原计划"复现旧锚点+top6"降级为"新建本仓锚点" | — | 旧锚点表保留于 PRINCIPLES §5 作历史参考，标注"未绑 commit，仅参考" | 用户裁定 + UPSTREAM.md pin 规则
- [EXP-001a 结果] 2026-09-02 | 口径 | **ANCHOR-003 = 64958 ± 137.7（SE）**（525 计划×1 局，std 3155，自选未达标 0/525）@pin 43f532c / gamedata 4a098eb1 / seed 61444 | ramen_space_bench（run 33578660083 ✅，EXP-001a-artifacts） | ✅ | 本仓正式锚点；旧 65445（未绑 commit）差 −487 → 维持历史参考；seed 方差未测（每计划 1 局），NN 配对验收若需补 EXP-001b（8 rep≈12s）；实测 525 局=1.4s → 模拟不是瓶颈 | results.md
- [EXP-002 立项（矩阵版）] 2026-09-02 | 数据 | pin 43f532c 教师采集 12000 条：**Actions strategy.matrix 8 并行分片 × 1500 条**（index 区间互斥；sample_position 按 index 确定性 → 并行 ≡ 顺序），search_n=8 / shard 256 / radical 1.4 / quota 20,30‰ / region=all 强制；每片单条冒烟后续跑拉满；assemble job 校验 recipe_hash 一致+区间无缝+重排合并 | exp-002.yml（8×collect + 1×assemble） | 待跑 | 产出 EXP-002-dataset 供 EXP-003 训练；wall-clock 进 LEDGER 对比顺序版 | 用户裁定（2026-09-02）：矩阵=CI 内并行分片 job（44 次手写迭代即此做法）；单 job 顺序 resume 是错误理解，作废
- [EXP-002 完成] 2026-09-02 | 数据 | **11837 条**教师样本（12000 计划 − 163 未捕获；8 片各 1471–1491，56 parts）；矩阵 wall-clock **3.2 min**（run 33585590880 ✅，9 job 全绿）；两次胶水事故：--count 累计语义误当终点（INCIDENT-20260902，区间断言拦截）+ assemble part 定位（PR #5/#6 修复），采集/校验本体零事故 | exp-002.yml ×3 运行 | ✅ | EXP-002-dataset artifact = EXP-003 直接输入；矩阵做法实测 ~5× 于单 job 串行 | results.md
- [EXP-003 立项] 2026-09-02 | 训练 | 上游 pin 全链路（本仓零新算法代码）：ramen_export_npy --raw → labels.py（Bayesian bootstrap 512 + cross-fit value）→ train.py **3 种子矩阵并行**（20260830/31/32，组合切分留出，早停监控期望后悔值）→ export_onnx.py（opset13 + 对拍 <1e-4）| exp-003.yml（1 convert + 3×train + 1 summary） | 待跑 | 产出 model.onnx ×3；Python 侧判读 vs 旧 pilot 148.8 同量级（>200 先查数据）；**最终裁决=EXP-004 Rust 闭环 vs ANCHOR-003，t>2 才放行（§0.4）** | plan.md；不做超参扫描/结构消融
- [EXP-003 完成] 2026-09-02 | 训练 | 3 种子 regret **296.4 / 305.7 / 327.0（均值 309.7）**，早停 103–129ep，308k params，ONNX 对拍全过 vs 旧 pilot 参考 148.8 → **触发排查闸门（mean>200=先查数据/标签不许下结论）**；训练机制健康、产物有效 | exp-003.yml（run 33588447596 ✅，全流程 7.6min） | ✅ | EXP-003-model-seed* ×3 直接可用；主嫌疑=search_n=8 rollout 列数不足（bootstrap 趋近 one-hot + crossfit 方差大）；summary 路径 bug 第 4 起胶水事故（artifact v4 剥目录层，PR#8 修） | results.md
- [EXP-003b 立项] 2026-09-02 | 排查 | H3b=rollout 列数不足。两线：①label-stats 对拍（labels.json entropy/stability/value 常数 vs DESIGN 公布 pilot 数值，零采集成本）②**search_n 剂量对照**：sn∈{8,32,64} × 同 [0,3000) 区间 × 2 种子（扁平矩阵 24 collect + 合并转换 + 6 train）| exp-003b.yml | 待跑 | 判定：降幅 >10%（超种子散布）→ H3b 成立正式采集升档；否则转查协议/分布 | plan.md；组间可比前提=同 index 区间同局数
- [EXP-004 立项] 2026-09-02 | 闭环 | **最终裁决场**（§0.4）：ramen_space_bench --trainer nn（feature onnx，race_shield on，special_mode=canonical）矩阵手写+3NN × runs_per_plan=8 = 4200 局/组；同 (计划,局) 共享 rule_master 种子流 → CSV 行序+seed 列双重配对校验；**t>2 且 Δ>0 才判 NN 超手写**；同时标定「regret 309.7 ↔ 闭环 Δ」映射 | exp-004.yml（4 bench + 1 summary） | 待跑 | ANCHOR-003 是 1 局/计划口径（seed 方差未测），本次 8 局/计划补足；NN 劣化预期如实记录 | plan.md
