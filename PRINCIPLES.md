# 拉面杯 NN 迭代准则 v1

> 依据：对 `muxueliunian/umaai-rs-muxue@feat/ramen-nn-schema` 全量审计（DESIGN.md + changelog 2026-08-13→08-31 + flat_search.rs/tests）+ 30GB 剖析事故复盘。
> 本文件是实验治理宪法：与代码冲突时，先改这份文件并说明理由，再改代码。

## 0. 铁律（违反 = 实验作废，无论结果多好看）

1. **只认闭环**。模型排序/验收唯一口径 = Rust 侧整局闭环基准（`ramen_space_bench` 同分布口径）。任何离线指标（expected_regret、留出 KL、验证 loss）不得用于排序训练方案——历史已实锤：expected_regret 算在错误分布上，与闭环结论**反向**（作者 2026-08-31 自曝并用 on-policy 配对 advantage probe 修复）。
2. **基线同局可比**。每次跑基准，手写基线（`RecommendedRamenTrainer`）与待测策略必须**同一 commit、同一 config、同一批种子**。跨 commit 比较均分 = 无效实验——上游 changelog 连续五次「改变拉面模拟数值，基线作废」就是这条的尸体记录。
3. **采样空间一致性**。评测分布必须与教师数据采样空间同构（(马娘, 卡组) 组合）。禁止用 `bench_base` 的局外马娘/无自选比赛口径给网络验收（上游明确踩过）。
4. **没有 t 检验不下结论**。所有"XX 比 YY 好"的声明必须附：均分、配对差值 ±95% CI、t 值、局数。t < 2 → 只能说"不可区分"。旧 ens6(v5) 的 +647±65.6 t=9.9 曾带着未声明的混淆上线——混淆未查清的模型永不复用。
5. **账本 append-only**。`experiments/LEDGER.md` 只增不改不删。推翻旧结论写新条目引用旧行号，禁止抹掉。

## 0.5 上游关系（用户裁定，2026-09-01）

- 本仓是**独立仓库**，上游代码以 CI checkout 方式**只读**引入，永不 fork-commit、永不向上游提 PR。
- 迭代规模会很大，一切修改（哪怕只是修上游 dead_code）都落在本仓自己的分支/实验目录。
- 上游引用 = `experiments/UPSTREAM.md` 里的**固定 commit SHA 白名单**；换 pin = 新实验条目 + 基线重测（§0.2 同理）。
- 上游行为变化需在本仓复现时，用 patch 目录（`patches/`）管理，不维护上游分支副本。

## 1. 30GB 教训 → 测量协议三步验证

事件：性能剖析提议直接上火焰图，结果剖析数据拉了 30GB 硬盘还是定位不到瓶颈，换方案后才测出（上游同期：pprof Windows 编不过收进 feature、自写 sim_profiler、rollout 消息屏蔽白拿 -29% CPU）。

**任何新测量协议（剖析/采样/基准）先过三步，再投入正式算力：**

1. **冒烟**：最小规模（1 局/100 样本）跑通全链路，确认数据落盘格式正确、指标非零非 NaN。
2. **体量预估**：先算 `单样本字节 × 预期样本数`，超过 CI 磁盘 1/3（约 4GB）必须有分片+清理方案。`--raw` 全量 rollout 导出默认禁用于 CI。
3. **分辨力自检**：用已知不同的两个配置（如 search_n=32 vs 64）跑一遍，确认新协议能测出已知差异。测不出已知差异的协议是坏的，换方案，不要加样本量硬怼。

## 2. 数据与标签纪律（承接 DESIGN.md，绑定 CI）

- pilot 结论（11957 条：attention 选型、value 归一化常数 center/scale）**一律视为过期先验**——它们没绑定模拟器 commit，而 8/23-8/27 五次模拟数值变更加上 8/31 SpecialSelect 还原，全部改变分布。CI 从零重采，pilot 只用于设计假设。
- value 三路口径（cross-fit mean/std/rf 中点积分）与反归一化（`center + scale*output`，stdev 截非负，禁用温泉 `scale*abs`）不得私改；改 = 新实验条目 + 全量重训。
- 稀疏阶段（SuperRamenSelect 42 条）不得用训练权重粉饰泛化——采集端必须用地区配额机制定向补样本，评估报告强制携带阶段样本数，阶段样本 <500 时该阶段指标标注 [insufficient]。
- 等价候选不合并不删格位（防止枚举顺序成隐式监督）；RegionSelect 边缘化口径冻结。
- 数据 manifest 必须记录：gamedata 签名、模拟器 git commit、采集配方哈希、种子区间。缺一 = 数据作废。

## 3. 分方向迭代规则（用户 + Claude 各走一路，互不阻塞）

- 每个方向 = `experiments/` 里一个编号目录（`EXP-NNN-<slug>/`），含 `plan.md`（假设/方法/验收标准，先写后跑）、`results.md`（跑完填，附 CI run 链接）、账本一行。
- **并行不互斥**：同一代模型允许多方向并行实验；方向间冲突（同数据同模型改不同超参）用实验 ID 在账本登记顺序裁决，后跑的引用先跑的。
- 负结果一等公民：假设被证伪照常归档，写明"哪条假设死了、证据是什么"。
- 代码改动全部落本仓：模拟器级修改进 `patches/`（apply 在 CI pin 的上游 checkout 之上），训练侧修改直接进本仓 `scripts/`；任何 patch 必须带本仓复验测试，不依赖上游评审。

## 4. CI 训练约束（公开仓库，runner 免费档）

- 单 workflow ≤ 6h（GitHub 免费档单 job 上限），训练任务必须支持 `--max-train-samples` 嵌套抽稀断点续跑；缓存 = labels + checkpoint（key 绑 gamedata 签名 + 配方哈希）。
- gamedata 下载沿用主仓 build.yml 方案；构建产物只保留 `model.onnx` + `model.onnx.json` 元数据，中间 .npy 用完即删。
- 每周末定时 job：采样配额补稀疏阶段 + 全量重训 + 闭环验收，结果自动 append 到账本（bot 提交）。
- ONNX 导出保持 opset 13 + 算子白名单 + batch 1/7 对拍 <1e-4，这是手机端 JNI 消费的契约，动它 = 破坏下游。
- 免费档算力边界（实测确认）：4 vCPU/16GB/无 GPU——模型量级（≤245 输出、≤20 token attention）CPU 即可；瓶颈在教师采集与闭环基准，靠分片+断点续训覆盖；真需 GPU 时走 Colab/Kaggle，不在 Actions 付费。

## 5. 当前账本锚点（2026-08-31 快照，来源：用户截图，口径=闭环配对 vs 手写）

| 模型 | 均分 | 对手写 | t | 状态 |
|---|---:|---|---:|---|
| 手写基线 | 65445 | — | — | 锚点 |
| P1 seed4 单模型 | 66818 | +1373.2±60.7 | 22.6 | 有效 |
| 同代集成 top3 | 66936 | +1491.1±60.1 | 24.8 | 有效 |
| 同代集成 top6 | **66949** | +1504.7±61.8 | 24.4 | **当前最优** |
| 同代集成 all12 | 66786 | +1340.9±61.2 | 21.9 | 有效（欠采样解释） |
| 旧 ens6(v5) | 66092 | +647.1±65.6 | 9.9 | **作废：v5 有混淆，禁复用** |

⚠ 上表来自旧管线（pilot 时代，未绑 commit）。CI 首跑（EXP-001）必须用当前 commit 重测手写基线，对不上则锚点重置。集成复现已取消（checkpoint 不可得）。

## 6. 七万分路线（方向地图，均分口径）

现状最优 66949 → 目标 70000（缺口 ~3050）：
1. **特征/价值侧**（用户方向）：SpecialSelect 还原已上线但样本为零 → 地区配额补采后重训；二代 leaf 语义对齐（V^π vs 搜索改进一步）是最大单点风险。
2. **搜索侧**（Claude 方向）：NN leaf 接入 MCTS 截断估值（max_depth>0 路径已存在于 flat_search，目前 onsen 专用）——手机端 4096 次 rollout 的算力瓶颈可被 leaf 估值大幅释放。
3. **口径侧**（两端共同前提）：种马因子硬编码校准、URA buffs(Category=4) 消费、换皮 id 全链路——模拟器不诚实，一切均分都是空中楼阁（主仓 v0.4.2 已修大半）。
